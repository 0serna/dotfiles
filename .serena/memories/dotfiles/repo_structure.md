High-level structure:
- `src/` - TypeScript source for the installer/linker and helpers
- `dotfiles/` - managed files to be linked into the user system, including `dotfiles/opencode/`
- `openspec/` - specs and archived change records
- `README.md`, `package.json`, `tsconfig.json`, `biome.json`, `dotfiles.json` - project config and manifest